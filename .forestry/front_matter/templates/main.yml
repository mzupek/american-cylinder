---
label: Main
hide_body: false
fields:
- name: main_header
  type: text
  config:
    required: false
  label: Main Header
  description: Add the main site header
- name: main_sub_title
  type: text
  config:
    required: false
  label: Main Sub Title
  description: Add your main sub title
- name: main_background_image
  type: file
  config:
    maxSize: 64
  label: Main Background Image
  description: select a background image
